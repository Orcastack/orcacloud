apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker
  namespace: orcacloud
  labels:
    app: celery-worker
    component: worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: celery-worker
  template:
    metadata:
      labels:
        app: celery-worker
        component: worker
    spec:
      containers:
      - name: celery-worker
        image: ghcr.io/orcacloud/orcacloud-backend:1.0.0
        command: ["celery", "-A", "orcacloud", "worker", "--loglevel=info", "--concurrency=4"]
        envFrom:
        - configMapRef:
            name: django-config
        - secretRef:
            name: django-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "celery -A orcacloud inspect ping"
          initialDelaySeconds: 30
          periodSeconds: 30
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-beat
  namespace: orcacloud
  labels:
    app: celery-beat
    component: scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-beat
  template:
    metadata:
      labels:
        app: celery-beat
        component: scheduler
    spec:
      containers:
      - name: celery-beat
        image: ghcr.io/orcacloud/orcacloud-backend:1.0.0
        # Avoid importing django_celery_beat at process startup until migrations
        # are applied; run plain beat here and enable scheduler after migrations.
        command: ["celery", "-A", "orcacloud", "beat", "--loglevel=info"]
        envFrom:
        - configMapRef:
            name: django-config
        - secretRef:
            name: django-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flower
  namespace: orcacloud
  labels:
    app: flower
    component: monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flower
  template:
    metadata:
      labels:
        app: flower
        component: monitor
    spec:
      containers:
      - name: flower
        image: ghcr.io/orcacloud/orcacloud-backend:1.0.0
        command: ["celery", "-A", "orcacloud", "flower", "--port=5555"]
        env:
        - name: FLOWER_PORT
          value: "5555"
        ports:
        - containerPort: 5555
        envFrom:
        - configMapRef:
            name: django-config
        - secretRef:
            name: django-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: flower
  namespace: orcacloud
  labels:
    app: flower
    component: monitor
spec:
  ports:
  - port: 5555
    targetPort: 5555
    name: http
  selector:
    app: flower