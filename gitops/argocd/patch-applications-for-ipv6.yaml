# ArgoCD Application to apply the IPv6 patches/Config
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orcacloud-ipv6-patches
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/orcacloud/orcacloud.git
    targetRevision: main
    path: k8s/patches
  destination:
    server: https://kubernetes.default.svc
    namespace: orcacloud
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  syncOptions:
    - CreateNamespace=true
